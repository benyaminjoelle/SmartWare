import 'package:flutter/material.dart';

class ClientSecurity extends StatelessWidget {
  const ClientSecurity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}