import 'package:flutter/material.dart';

class ClientEditProfileView extends StatelessWidget {
  const ClientEditProfileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}