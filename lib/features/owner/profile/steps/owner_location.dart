import 'package:flutter/material.dart';

class OwnerLocation extends StatelessWidget {
  const OwnerLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}