import 'package:flutter/material.dart';

class OwnerAnalyticView extends StatelessWidget {
  const OwnerAnalyticView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}